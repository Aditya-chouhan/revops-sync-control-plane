revops_sync_warehouse:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID', 'replace-with-authorized-gcp-project') }}"
      dataset: analytics_revops
      location: asia-south1
      threads: 4
